import urllib2
import urllib
import sys
import config as c
import os
import subprocess

def get(player):
    photodir = os.path.join(c.BASEDIR,"images/players/")
    photo = ""

    # # Start custom code
    # 1. Store a photo for passed in player in photodir
    # 2. return the filename (no directory parts included)


    # # end custom code



    return photo
